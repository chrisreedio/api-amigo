<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void
    {
        $tablePrefix = config('api-amigo.table_prefix');
        Schema::create($tablePrefix . 'endpoints', function (Blueprint $table) use ($tablePrefix) {
            $table->id();

            $table->foreignId('connector_id')
                // ->nullable()
                ->index()
                ->constrained($tablePrefix . 'connectors')
                ->cascadeOnDelete();

            $table->string('method');

            $table->string('name')
                ->nullable();

            $table->string('class')
                ->nullable();

            $table->string('path');

            $table->decimal('average_duration', 10, 3)->nullable()->comment('In seconds');

            $table->timestamps();
        });
    }
};
