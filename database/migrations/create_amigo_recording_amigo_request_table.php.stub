<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        $tablePrefix = config('api-amigo.table_prefix');
        // Schema::create($tablePrefix . 'recordings', function (Blueprint $table) use ($tablePrefix) {
        Schema::create('amigo_recording_amigo_request', function (Blueprint $table) use ($tablePrefix) {
            // $table->id();

            $table->foreignId('recording_id')
                ->constrained($tablePrefix . 'recordings')
                ->cascadeOnDelete();

            $table->foreignId('request_id')
                ->constrained($tablePrefix . 'requests')
                ->cascadeOnDelete();

            // $table->timestamps();
        });
    }
};
